import React from "react";
import './RiskNumber.scss';

const RiskNumber: React.FC = ({children}) => (
    
    <div className="risk-number">
        {children}
    </div>
)

export default RiskNumber;